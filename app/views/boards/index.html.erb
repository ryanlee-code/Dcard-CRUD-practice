<h1>看板標題</h1>
<%= link_to '新增看板', new_board_path %>
<ul>
  <% @boards.each do |board| %>
    <li>
      <%= link_to '修改', edit_board_path(board.id)%>
      <%= link_to "刪除",board ,method: 'delete', data: {confirm: "是否刪除"}%>
      <%= link_to board.title, board %>
    </li>
  <% end %>
</ul>